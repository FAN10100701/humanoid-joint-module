# 评论区现状(2026-08-17 最终决定)

> **结论:云端评论停用,评论区替换为「联系站长」卡片(站长邮箱 2061624805@qq.com)。**

## 为什么停用

免费云端评论方案的完整排查记录(均已实际验证):

| 方案 | 结论 |
|---|---|
| Giscus(giscus.app) | 后端被墙,放弃 |
| Twikoo + CloudBase 云函数 | 已打通 CORS/鉴权/代码补丁,但**体验版无文档数据库集合配额**(PostgreSQL 有但 Twikoo 不用),创建集合需付费 |
| CloudBase 升级付费版 | ≈¥6.9/月起,用户暂不付费 |
| Zeabur 部署 Twikoo | 免费额度已收紧,需付费 |
| Valine + LeanCloud | LeanCloud 已停止新用户注册 |
| 自建服务器 | 需备案+月费,暂不考虑 |

## 当前实现

- `_assets/giscus-config.js`:`provider: "disabled"` + `email: "2061624805@qq.com"`
- `_assets/site.js` `mountComments()`:provider 为 disabled 时渲染「📮 联系站长」卡片(mailto 直达邮箱),不再加载任何评论脚本(零网络依赖、零报错)
- 所有 `.container` 页面自动生效,无需逐页修改

## 未来恢复方法(任选其一)

1. **开通 CloudBase 个人版**(≈¥6.9/月):环境 → 套餐升级 → 数据库(文档)创建 `comment`/`config` 集合 → `giscus-config.js` 改 `provider:"twikoo"` + envId 填完整网关地址(云函数 CORS 补丁已就绪,保留)
2. **Twikoo 私有部署**:部署后把 envId 换成自托管地址,`provider:"twikoo"`
3. **Valine**:LeanCloud 若能注册,`provider:"valine"` + appId/appKey

> 云函数补丁(文件末尾 CORS + HTTP 网关适配)是恢复 Twikoo 时的必要条件,已存档于本目录《腾讯云AI操作指令_Twikoo评论修复.md》。
