# 图片问题迭代循环 · 修复摘要(2026-09-06 · 第二轮)

- **执行依据**:`docs/维护/图片迭代循环提示词.md` + svg-diagram-check skill(rules.md / taxonomy.md 全文已读);用户指令「再次审查修改一轮每个页面的图片」
- **范围**:全站 37 个含 SVG 页面,浅色+深色双主题逐页截图目检(8 项清单:可读/文字/箭头连线/框对齐/图注/颜色语义/公式/删一个元素)
- **起点台账**:E=0,W=2(与第一轮摘要终点一致;两条 W 经核验仍为误报,见文末)
- **终点**:E=0,W=2;板块评分九板块与整改后基线持平(10=91.7A / 09=86.6A / 06=86.3A / 03=85.5A / 07=84.1 / 02=80.6 / 01=79.2 / 00=69.0 / 08=61.6),风险页 0;一键自检 33 项中 32 项 PASS(C7 隐私扫描 4 项均为 `.mimosa/` 工具目录本地未跟踪记录,非站点交付物,与本轮无关,未处置——见文末)
- **净修复:约 54 处逐处修复 + 311 处批量规范化 + 1 次结构抢救**(单页均 ≤5 轮)

## 一、逐处修复明细

### 00_3D解剖/减速器剖面图详解(11 处)
| 图 | 问题 | 修法 |
|---|---|---|
| 图1 谐波 | 波发生器蓝引线端点 (60,120) 落入副标签「椭圆凸轮+柔性轴承·输入端」内 | 端点改 (66,132),止于文字下方 6px |
| 图1 谐波 | 啮合区红线端点距文字 4px、刚轮灰线端点距文字 2px | 端点各退至 6px 间隙 |
| 图2 行星 | 太阳轮蓝色小线段端点 (210,12) 顶进「高」字字形区 | y2=12→20 |
| 图2 行星 | 行星架黑线端点 (60,220) 落入「行星架(输出·低速)」内 | 端点改 (66,232) |
| 图2 行星 | 行星轮/内齿圈引线端点距文字 2px | 各退至 6px |
| 图3 摆线 | 曲柄销孔黑线端点 (60,240) 落入「曲柄销孔(输出法兰)」内 | 端点改 (60,252)(避开针齿圆) |
| 图3 摆线 | 摆线盘/针齿圈引线端点距文字 2px | 各退至 6px |
| 图3 深色 | 「偏心轴(输入)」在橙色摆线盘上,深色主题被全局覆盖规则变浅字压橙底不可读 | 新增 `.svgbox text.on-disc` 双主题类(深色下 #78350f 深棕) |

### 01_理论入门/03_驱动电路模块认知(15 处)
| 问题 | 修法 |
|---|---|
| 交互区左右分栏把 1100×680 画布压进 464px,SVG 内 11/9px 标签实渲染仅 4.6/3.8px,P1 不可读 | `.my-interactive` 改上下堆叠,画布占满全宽(918px),详情面板移至下方 |
| 画布全宽后仍偏小 | SVG 字号一档上调:label 11→13、sub 9→10.5、wire/voltage 10→12(逐盒核算不溢出) |
| 「3.3V/5V」红标压电源管理盒右下角 | 移至竖线右侧 (396,170),避开 MCU 盒 |
| 「电流反馈」绿字大半压在主控 MCU 盒右缘 | 拆两行「电流/反馈」置于竖线右侧 40px 走廊内 |
| 「故障状态」黄字戳进 MCU 盒左缘 10px | 拆两行移至走线下方空档 |
| 绿色编码器走线 (x=905) 直接穿过「抱闸驱动」盒(y 280-360) | 走线绕行右侧 (1002) 再入编码器盒顶 |
| 「电机端编码器」绿字压抱闸盒底边 | 重位到走线旁 (800,264) 空档带 |
| 「输出编码器」绿字压抱闸盒内部 | 标注随绕行走线移至右缘 (1008,320) |
| 「总线接口/4pin/A/B」灰字压通信接口盒内(浅色主题下 #5a6a85 压深青底难读) | 连接点移至盒左缘 (200,265),标注移出盒外左侧 |
| 「PWM×6」紫字左半压 MCU 盒顶右角 | 上移 y195→168 |
| 「U/V/W」橙字下半压三相全桥盒顶边 | 上移至紫/橙盒间 20px 空档 y204 |

### 06_软件与算法/17_状态观测器(6 处)
| 图 | 问题 | 修法 |
|---|---|---|
| 图① 龙伯格 | 反馈线末段从 ×L 盒内部穿出,仅露 14px,箭头紧贴观测器盒 | 线改为进盒顶中心 (390,236),出盒段独立画线带箭头(342,252) |
| 图③ SMO | 红字「e = i − î(滑模面 s=0)」被蓝色 î 反馈竖线 (x=130) 穿过 | anchor 改 end x=124,移至线左侧 |
| 图③ SMO | 「θ̂ ω̂ → Park/速度环」被绿色输出竖线穿过 | 竖线缩短 y214→192,止于文字上方 |
| 图③ SMO | 「开关项 z」引线悬空裸收尾 | 延长至电流观测器盒顶并补 marker |
| 图④ HFI | 曲线为 cos(2Δθ) 形状(峰在 Δθ=0),标注却写「∝ sin(2Δθ) 解调信号」,数学矛盾 | 标注改「幅值 ∝ cos(2Δθ)」(源码注释与「Δθ=0 幅值最大」标注均佐证 cos) |
| 图④ HFI | 「磁阻大→Ld 小」「磁阻小→Lq 大」与红色虚线弧/转子圆边多处纠缠(逐一推算无同时避开三者的空位) | 删圆内两标签,合并为图下双行注(红=d 轴、绿=q 轴,与轴色对应) |

### 06_软件与算法/19_FOC 算法完全图解(1 类 3 处)
| 图 | 问题 | 修法 |
|---|---|---|
| 图④ 三环级联 | 三个绿色 ▲ 反馈箭头嵌在「≈10~100Hz / ≈100~500Hz / ≈0.5~2kHz」文字行内(箭头尖 y96 与字形区 96-108 相交) | 三处文字改 start 锚并右移至竖线右侧(x+8) |

### 06_软件与算法/20_工程电机控制框架对比(5 处)
| 图 | 问题 | 修法 |
|---|---|---|
| 图③ ODrive 状态机 | 「requested_state」尾部压入 FULL_CALIBRATION 盒 11px | 上移 y118→93 至盒顶上方 |
| 图③ ODrive | 绿色上行竖线 (x=96) 直接穿过「上位机接口层」虚线盒 | 走线沿盒右侧绕行 (196,172→292) |
| 图③ ODrive | 「再次上电可直接 CLOSED_LOOP(配置 startup)」尾部伸入闭环大盒 145px 并与盒内标题咬合 | 拆两行 end 锚置于竖线左侧 |
| 图⑨-1 选型树 | 「MIT·不绑硬件」行超出 SimpleFOC 盒底 6px(静态审计只查画布边界,盒边界漏网) | 盒高 46→62 |
| 图⑨-1 选型树 | 「以上都不满足…」灰字与产品化盒下来的斜虚线相交 | anchor 改 end x=294,移至竖线左侧 |

### 08_学习工具/05_传动与控制实验台(2 处)
| 问题 | 修法 |
|---|---|
| 780 宽 SVG 被栅格左列压到 453px(缩放 0.58),12px 字实渲染 7px 不可读 | `.lab-grid` 改单列,SVG 全宽(894px) |
| `.lab-svg` 背景 55% 半透明深色叠加浅色页面合成中灰,SVG 内 #64748b/#8b949e 灰字对比仅 ~1.5:1 | 背景改不透明 `#0a0f16`(与同页示波器画布一致,双主题深底仪表盘风格) |

### 10_NPU与数字IC设计(11 处)
| 页·图 | 问题 | 修法 |
|---|---|---|
| 10-02 工具链 | ③④ 盒副文字溢出盒宽(vcs…245px / $fsdbDump…272px > 盒 240px,两侧各超 16px) | 缩短:「vcs -full64 -kdb -R 一步到位」「$fsdbDumpfile / $fsdbDumpvars」 |
| 10-05 APB | 图④ 底部图例①(x 31-168)与②(x 127-282)同 y 重叠 41px | 三段错开 x=100/255/430 |
| 10-06 LeNet | 图② OUT 盒与 F6 盒交叠 10px | OUT 盒右移 30px,viewBox 560→590(规范允许的最后手段) |
| 10-06 LeNet | 图② 两条分布条 (x=160) 与左侧说明文字尾部 (至 169) 咬合 | 分布条区段整体右移 26px |
| 10-07 脉动阵列 | 图③ 金字塔顶层主行文字两端与梯形斜边相切(±0.2px) | 字号 12→11 |
| 10-08 量化 | 图① 底部两条标注同 y 重叠 49px | 拆两行右置 (490,277/296) |
| 10-10 NPU实战上 | 图① fc 直送横线穿过「fc 层: 特征图展平直送全连接…」标注 | 文字下移 y428→444 |
| 10-11 NPU实战下 | 图⑤ 回环绿字 (y132) 与回环曲线谷底 (y≈130) 相交 | 下移 y=145(距画布底 2.5px) |
| 10-12 SV/UVM | 图② agent 副文字 213px 溢出盒宽 (164px) 两侧各 24px | 盒加宽 164→216 |

## 二、批量规范化(311 处):深底图卡内次要灰统一

10 板块页级 `.my-fig` 为刻意深底设计,但 SVG 内文字用浅底钦定灰 #64748b,在合成深灰底上对比仅 ~1.7:1。按 rules.md「深底专用次要灰 #8b98a9」与 V2.1.16「深底卡页 #8b98a9」先例逐页判定处理:

- **A 类恒深底**(无浅色主题覆盖,02/03/04/09/12/13):SVG 内 `fill="#64748b"` → `fill="#8b98a9"`,共 **88 处**
- **B 类双主题图卡**(有 `html:not(dark) .my-fig` 浅色覆盖,05/06/07/10/11):一度误改 223 处后**精确回改** `#64748b`(白底可读;深色主题下深底 3.3:1 尚可,与历史状态一致);回改数与正向数逐页对账(22+43+34+45+79=223)✓
- **浅底页**(01/08,`.my-fig` 无背景定义):首轮误改 18 处,git 恢复

## 三、结构抢救(06 LeNet 页,并行会话事故善后)

全站终审时 06 页突发 E=3(svg 未配平 4 开 5 闭 + mC1/mC2 marker 重复)。排查确认:**并行会话的编辑事故**——`</html>` 之后拖有 40,862 字符幽灵重复内容(图②尾碎片 + 完整重复的「反向传播链路图」+ 图③ cap;关键内容 grep 均出现 2 次,正文有正本)。浏览器会容错渲染这段重复内容。处理:确认正文完整后截断 `</html>` 之后全部内容,3 个 E 全部消除。⚠️ 请并行会话知悉,其后续编辑请基于截断后的文件。

## 四、误报/澄清清单(本轮未改,源码核验正确)

- 00_3D svg#2/#3「缺图注」:图注存在,距 `</svg>` 352/348 字符超出脚本 300 字符扫描窗口
- 02-04「测 3.3V…」、03-06「抱闸·测试点未入图」「120Ω 预留」、06-03「QoS 逐话题权衡…」、08-09「¥5000+,先吊挂!」、09-01「(其余 248 位休息)」、10-10「乒乓选择」、10-12「active:driver+sqr+mon」、10-13「monitor 旁路采样」「irq(电平,ie 门控)」——均为截图小字模糊导致的误读,源码全部正确

## 五、遗留事项

1. **C7 隐私扫描 4 项 HIGH**:`.mimosa/` 工具目录下 4 个 JSON 含「sk-review-se…」形态字符串(2026-09-05 Mimosa 扫描本地记录;目录未被 git 跟踪、从未提交)。非站点交付物,建议后续将 `.mimosa/` 加入 `.gitignore` 或由工具 Owner 清理——本轮未动(非图片范围,且可能为并行工具在途数据)。
2. 10 板块 B 类双主题图卡(05/06/07/10/11)深色主题下 SVG 灰字 #64748b 对比 3.3:1,如需进一步提对比可考虑页级 `.my-fig text` 深色主题覆盖规则(本轮未动,避免口径扩张)。

## 六、第三阶段:全站隐含问题查找 + 美观协调(本轮追加)

### 6.1 全站结构隐含扫描(119 个 html)
自写扫描器(`docs/审计/_结构扫描_第二轮.js`):`</html>` 之后残留内容、多重 `</html>`、div 配平、图注/figcaption 重复、svg aria-label 重复。结果:
- **06 LeNet 的幽灵内容是全站唯一结构损伤**(已修复);其余 118 文件干净
- 08-03 自测题库 div 计数差 1 为 JS 模板字符串拼接误报(运行时 DOM 配平)
- `baidu_verify_*.html` 无 `</html>` 为百度验证文件正常形态
- `target="_blank"` 1223 处全部已带 noopener ✓

### 6.2 🔴 全站性能隐疾:busuanzi 计数器阻塞 load 事件(已修)
子页加载实测 10.2s,根因:`site.js` 页脚注入的 `busuanzi.ibruce.info` 计数脚本(第三方,近年经常不可达)虽为 async,**仍阻塞 window.load 事件**,服务不可达时每个子页 load 被拖 ~10s(首页不注入所以幸免)。修法:注入推迟到 `window.load` 之后(计数到达后照常回填页脚,永不阻塞加载)。修后实测 **275ms**。该问题与图片无关但属"全站隐含问题"最高价值发现。

### 6.3 美观协调:图注编号连续性(已修)
全站 102 条图注普查(`docs/审计/_图注顺序调查.js`):跨页存在「图①圈号 43 / 图N无冒号 28 / 图N:冒号 24」三种板块级历史风格(各自页内一致,强行统一 102 条风险大于收益,未动);单图页 6 处「图:」无编号属合理惯例;10-07 第五条「表·LeNet 逐层硬件映射」为表格注释的合理语义化前缀。
**页内编号连续性检查抓到 3 页真缺陷**(`docs/审计/_图注重编号.js` 占位符置换法重编,正文交叉引用同步):
- 06-16 MPC:文档顺序 ⑤③②④ 且缺 ① → 重编 ①②③④,正文 7 处引用同步
- 10-05 APB:文档顺序 ④③①②⑤ → 重编 ①②③④⑤,正文 9 处引用同步
- 10-06 LeNet:结构图误标图①(与卷积图重号)→ 文档顺序 ①②③,正文引用同步

### 6.4 移动端体验(已修)
390px 视口下 01-03 画布 SVG 缩至 321px,标签回到不可读区间——补 `@media (max-width:640px){ .my-canvas-panel{overflow-x:auto} svg{min-width:640px} }`(与 10 板块 `.my-fig svg{min-width:560px}` 同一模式),画布 640px + 横向滚动,机制实测生效。

### 6.5 交互功能冒烟(改过布局的页面,全部通过)
- 01-03:点击「主控 MCU」→ 详情面板正常填充(标题/7 张芯片卡/正文 3903 字符)
- 08-05:三个实验台 tab 切换正常,切回后传动链 SVG 878px(单列修复生效)
- 子页 load 从 10.2s → 275ms(busuanzi 修复)

### 6.6 澄清
- 07-07/07-08「缺 lang」为扫描脚本只读前 600 字符的误报,两页实际均有 `lang="zh-CN"`
- 10-05 图注编号「乱序」实为按叙述顺序编号的存档状态,本轮按文档顺序重编后与全站惯例对齐
