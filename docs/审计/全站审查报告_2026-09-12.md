# 全站审查报告(2026-09-12)

> 触发:用户要求"指出网站问题、寻找异常和隐藏 bug、用户体验/图片/3D 动画、内容与知识点是否足够、优化与美观建议、工程级安全防漏洞、本地知识库与后端选型、四专业教科书化路线,并修复人形机器人 3D 又显示不出来"。
> 本轮原则:**内容只增不删**;修复不动教学正文;所有"免费"结论当日实时核实并标注日期。
> 关联:A-57/A-58/A-59/A-60(AUDIT.md),V2.1.28 发版。

---

## 一、本批已修复(全部经一键自检 + 本地验证清单)

### 1. 3D「人形机器人又显示不出来」——根因:双 SW 缓存互删(A-57)
- 根 `sw.js` activate 删所有非当前 CACHE 名缓存 → 每次站点发版清空 `robot-3d-models-v1`(16-18MB 模型)与 `robot-3d-vXX-*`(three.js 库);反向 `00_3D解剖/sw.js` activate 删 `hrl-site-*`。CacheStorage 源级共享,两 SW 互不知晓 → 每次发版后 3D 页被迫全量重下,弱网下引擎 25s 超时/模型挂起。
- 修复:缓存所有权约定(根 SW 只清 `hrl-site-*`,3D SW 只清 `robot-3d-*`,模型缓存独立名永保留)+ `SW_VERSION` v30;**防回归:自检 C14**。
- 伴随(A-60):`'9'` 死端标记加 30 分钟 TTL(不再锁死标签页会话);URDF fetch 加 20s 超时+重试 1 次(弱网/本地服务器僵死不再无限挂起,仍走参数化模型兜底)。
- 用户验证清单:见发版说明(首页触发 activate 后 Cache Storage 里模型缓存仍在;强制 `'9'` 验证过期)。

### 2. 安全:两处反射型 DOM XSS(A-58/A-58)
- `404.html`:`decodeURIComponent(location.pathname)` 未转义进 `innerHTML` → 改 `textContent`;
- `06_学习工具/13_AI答疑助手.html`:`?ctx=` 参数未转义拼 `innerHTML` → 走 `Site.esc`(留本地兜底);
- 危害:同源脚本可读 localStorage(含 `humanoid-ai-config-v1` 明文 API Key、全部笔记/进度)。**上后端前必修,已修**。防回归:自检 C14 定点断言。

### 3. 图片/SVG 全站审计
- `svg_audit.js --all`:**39 个含 SVG 页面,0E 0W**(错误/疑点双零);本批未改动任何图片,无需重截图。
- 3D 域:模型文件 URDF↔磁盘引用 100% 对得上,165 个 .drc 无空文件,代码缓存与磁盘一致——排除"发版没 bump 3D 缓存"假设。

### 4. 文字颜色对比度(WCAG,浅色+深色双主题)
- 新增零依赖检查器 `_本地工具/校验对比度.js`:CSS 变量按主题层解析、分组选择器拆分、同层背景优先继承、渐变按主题页底合成取中位色、@media print/prefers-color-scheme 剔除、双主题页基线规则"任一主题不过即报"。
- 全站审计结果与修复:**真实缺陷约 70 处修复**——
  - 浅色主题悬空浅灰 `#94a3b8`(2.4:1)→ 钦定灰 `#64748b`(≈4.5-4.8:1),site.css 约 10 处选择器 + 15 页;
  - site.css 状态/语义类只有深色配色:答对/答错反馈(`.correct/.wrong/.quiz-feedback/.srs-fb`)、`.srs-btn/.nav-done.on/.srs-tag`、导航图标钮、术语卡(term-card 浅色白底配白字 1.0:1)、`search-hit mark` 等 → 全部补浅色覆写;
  - 页级状态/标签类:`.my-rec/.my-beg/.my-tag-*(Hdrive 四色)/.my-kv-*/.my-copy-btn/.my-filter.on/.my-scale/.my-lic/.my-ds/.my-fw/.my-barfill/.my-checklist ::before/.my-sensor/.my-pri-*/.my-kv-d/.my-proj a/.my-spec/.my-en/.my-src/.my-note/.my-ref-tag` 等 12 页补浅色覆写(各页按自有机制 `html:not(...)` 或 `body[data-theme="light"]`);
  - 3D 主页 `.crumb .arr`(两主题 1.3)→ `var(--muted)`;知识问答"正确"标签浅色 → #15803d;FOC 动画页主按钮渐变加深(emerald-500/600→600/700,白字 2.5→4.5+);
  - `NEW` 徽章白字橙红渐变(2.15)→ 琥珀渐变 + 深棕字(5.3+);`.breadcrumb .sep`、`.nav-theme` 等定点修复。
- **残留 59 项,全部裁决归档,不属于缺陷**:
  - 3D 主页 `--faint` 装饰层(WebGL 视图标签/启动屏/图例小注,设计即"极浅")与强调钮白字(粗体 chrome 元素)——设计接受,历轮双主题截图目检在案;
  - 检查器已知边界误报:深色层背景继承未建模(3D 主页 `body[data-theme="dark"]` 覆写完备,代码逐一核实)、`::before` 标记继承卡片底色错配、悬停瞬态底色——均经代码核对非真问题;
  - 钦定灰 `#64748b` 在浅色页底 4.48:1(卡上 4.77 达标),边界记录在案。

### 5. 教学内容评测(只评测不删改)
- **板块评分五维**(报告:`docs/审计/板块评分报告_2026-09-12.md`,风险页 0):
  07 大模型 91.9(B) > 04 软件算法 89.1(B) > 05 前沿 88.5(B) > 03 实操 88.0(B) > 02 硬件 85.2(B) > 01 理论 84.2(B) > 08 NPU 81.8(C) > 00 3D 74.0(C) > 06 工具 68.7(C)。九板块无 D。
- **教学写法评测**(底表 `docs/审计/_教学评测底表.csv`):
  - 零比喻 53 页(深入浅出待加强,集中在硬件/算法工具型页面);
  - 零实例 19 页(缺算例/例子——与"题库定量题仅 12%"同根,见内容路线③);
  - 偏多 3 页(08 板块 3.5-4.1 万字,建议后续拆分或加导读);工具页过薄 3 页(12/14/15,工具性质,见 A-52)。

## 二、其余隐藏 bug / 隐患清单(发现未修,排队消化)

| # | 类别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| 1 | 内容硬错误 | 03-06 页 | 知识点评估报告 P0:3 处硬错误 | 内容轮随 worked-example 一起修 |
| 2 | 未完成态 | FOC 图解页(旧报告 P0) | 页面未完成态 | 内容轮补齐 |
| 3 | 零排错 | 大模型板块代码页(旧报告 P0) | 唯一代码页无排错内容 | 内容轮补 |
| 4 | 口径 | `AUDIT.md` | A-53/A-54 编号被"文档轮"与"工具轮"两轮复用(编号撞车);本批从 A-57 起跳避让 | 史实不改,台账加注 |
| 5 | 一致性 | 04 板块 | 两个 `20_` 重号文件 | 编号调整须走 dry-run 脚本(A-54 教训) |
| 6 | XSS 邻接 | `13_AI答疑助手.html:371` | 错误消息 `msg` 拼入 innerHTML(来源为异常文本,风险低) | 后端轮统一消毒层 |
| 7 | 存储型风险面 | `11_题库.html:488-505` 等 | 一方数据渲染**有意不转义**——静态时代安全,教师可编辑内容入库后即存储型 XSS 直通管道 | **上后端前必须补消毒层(阶段一前置)** |
| 8 | 兼容 | A-28/A-21/A-24 | KaTeX 离线不渲染 / CDN 无 SRI / localStorage 无迁移 | 台账既有开放项 |
| 9 | UX | quota 超限 | localStorage 写失败静默退化,用户无感知 | 加一次性 toast 提示(后端轮) |
| 10 | UX | 3D 页 | 模型失败形态有兜底+提示,但"重置加载"入口较深 | 诊断面板已有,观察期后再定 |

## 三、用户体验观察(总体健康,不列虚项)
- 离线能力完整(SWR + 预缓存 + 音频/模型持久缓存);搜索回显转义、URL 参数白名单(`?s=`/`?persona=`)安全;1223 处 `_blank` 全带 noopener;V2.1.27 音频预热/连点修复后朗读链路毫秒级;词卡面板命名空间事故(A-55)已收敛并有防回归约定。
- 本批 UX 修复:3D 失败不再锁死会话('9' TTL)、URDF 挂起有超时提示、浅色主题下状态反馈(对/错)恢复可读。

## 四、内容覆盖度与四专业教科书化路线(只增不删;按评估报告缺口顺序,已与用户确认)

现状:101 页/9 板块;机器人工程线最强(机构/电机/硬件/实操全链);评估报告 10 主线 8 条覆盖良好;结构性空档=具身数学层;经典机器学习全站零页;全站通病=worked example/数值算例少(题库定量 12%)。

**扩展顺序(路线图,后续独立会话执行):**
1. 经典机器学习层(决策树/SVM/无监督/评估指标)——AI 专业最大硬缺口;
2. 具身智能数学层(李代数/流形上的动力学/随机最优控制入门)——结构性空档;
3. 全站 worked example 补齐:控制线 MPC/KF/ADRC/WBC 数值算例链、零实例 19 页逐页消、题库定量题比例 12%→30%;
4. 机器人工程缺口:弱磁/MTPA、R/L 在线参数辨识、EMI/EMC、柔轮疲劳寿命、丝杠导程、CANopen/CiA402;
5. Agent 最小架构(感知-规划-工具-记忆循环)真讲解页;VLA 两页重叠约四成合并重写;
6. AI 深化:DQN/PPO clip/GAE 公式层、CV/NLP 学科基础课;
7. 教学写法:零比喻 53 页按"每页至少一个类比"逐轮消化;08 板块 3 个 3.5万字+页拆分/加导读。

## 五、美观建议与隐患

**建议(低成本高确定性):**
1. 字体维持系统栈,不引 webfont(中文体积/SW 缓存成本远大于收益);
2. 首页 hero 去"个人作品站"感:课程平台化(板块=课程编号卡、弱化个人时间线),个人化内容(作品台/保研线)收进"个人中心"分组;
3. 统一术语/公式/习题编号规范(图注已有规范,正文编号散);
4. 状态语义色已在本批对齐(绿=对/红=错/橙=警),新增组件沿用。

**隐患(为什么不能大改 CSS):**
1. 双主题回归面:每条颜色改动必须两主题验证——本批 70 处即是教训密度;
2. SW 缓存面:改 site.css 即全站生效,必须 bump CACHE(发版脚本管);
3. 词卡面板/注入样式命名空间(V2.1.25 事故):全局注入类名必须带组件根前缀,新增前全库 grep 排重;
4. 动图必过 svg-diagram-check;编号/结构调整必走 dry-run 可审脚本(A-54);
5. 结论:**美观升级走"只加不改 + 组件命名空间"路线,不做一次性视觉重构。**

## 六、工程级安全审计(四条链路)

| 链路 | 现状 | 结论 |
|---|---|---|
| 资源加载 | HTML 零外链 script/link;KaTeX 四级回退懒加载;three 本地优先+CDN 回退;不蒜子失败无残留 | 离线只有公式/ PV 退化,无功能挂;开放项 A-21(无 SRI) |
| 数据存储 | 全 localStorage/sessionStorage,主要读写点均有 try/catch;无 Cookie/IndexedDB;跨设备靠手动 JSON 导出导出 | 隐私模式静默退化(见二-9);A-24 迁移机制待做 |
| 渲染注入 | 单源 `Site.esc`;搜索回显/highlight 转义;URL 参数白名单;AI 输出先转义再重建标签 | 两处反射 XSS 本批已修;教师可编辑内容入库前必须消毒(见二-7) |
| 部署与密钥 | 全相对路径、零域名硬编码,GitHub Pages 子路径与 cyco.top 根双端皆可跑;无 CNAME;全库(含历史)无 `sk-` 形态密钥;AI Key 只存本机直连供应商;CI 仅自检 | 无单端故障面、无密钥入库;SEO 层 sitemap 指向 cyco.top 不影响功能 |

**结论:纯静态时代安全面干净;引入后端前的三件前置——①剩余 XSS 邻接点统一消毒层;②题库渲染管道转义契约;③localStorage 键版本化。**

## 七、"免费"真实性核实(全部 2026-09-12 当日实时抓取/查询,引用见文末)

| 方案 | 月成本 | 关键限制 | 免费真实性 | 国内访问 |
|---|---|---|---|---|
| **PocketBase 自托管** | **$0**(MIT,已验证 LICENSE 原文;最新提交 2026-09-07、61k stars、活跃) | 单文件 SQLite;内置账号/角色/文件存储/管理台;上线才需服务器(国内轻量约 ¥50~112/月) | ✅ 真免费,无用量/营收陷阱 | 优(数据在本机) |
| 腾讯云 CloudBase 个人版 | ¥19.9/月 | 新用户 0 元体验(页面标注 6 个月);**2026-01-16 起免费体验政策有调整公告** | ⚠️ 仅体验期免费 | 优,webapps 子域免备案 |
| Supabase Free | $0 | 500MB 库/5GB 流量/50k MAU;一周不活跃休眠 | ✅ 永久免费层仍在 | 不稳定 |
| Neon Free | $0 | 0.5GB 库/100 CU·时/月 | ✅ 永久免费层仍在 | 不稳定 |
| Cloudflare D1 Free | $0 | 5GB 库/每日 500 万行读/10 万行写 | ✅ 永久免费层仍在 | 一般 |
| LeanCloud 开发版 | $0 | 限开发/个人;云引擎半小时无请求休眠 | ✅ 开发版仍在 | 优 |

**开源许可红旗**(避免"以为免费其实有坑"):Directus、Outline = BSL 类非标准开源(GitHub NOASSERTION,2026-09-12 查),商用/对外服务前核对条款;SiYuan 思源 AGPL-3.0(本地免费,云同步付费);Obsidian 客户端免费闭源(Quartz 发布器 MIT)。
**结论:零成本且无休眠/政策风险的组合 = 现有静态站(GitHub Pages 免费)+ PocketBase 本地自托管——与用户选定路线一致。**

## 八、技术栈结论与三阶段路线(用户已确认:本地 PocketBase 先行)

**技术栈:不全面升级。** 静态内核(ES5/离线/免构建/双 SW)是本站离线能力与长寿命的根基,保留;教科书内容层新增模块允许现代 JS(独立 ES module 作用域,不污染存量页面);编辑器只在"内容层"生效,不触碰 101 页静态资产。

- **阶段一(本地闭环,用户逐项验证后提交)**
  1. 本地 Windows 跑 PocketBase(单二进制,`./pocketbase serve`);
  2. 数据模型:知识点/页面/图片/笔记四集合 + teacher/student 角色(collection rules:学生只能写自己的 notes,内容集合学生只读);
  3. 教学页「编辑模式」:页内富文本编辑、插图上传(PocketBase 文件存储)、自动整理格式(标题层级/公式/图注编号规范,复用 svg-diagram-check 的规范做校验);
  4. 学生笔记面板(基于 `docs/维护/笔记功能设计提案_2026-09-06.md` 三期方案落地);
  5. 前置:消毒层(二-7)。
- **阶段二:内容动态化 + 离线兜底**——教师编辑入库,定期导出静态 JSON 进 git;页面运行时 fetch 动态内容,离线/未部署时回退静态 JSON。
- **阶段三:部署选型**——CloudBase 个人版(¥19.9/月,国内快免备案)或国内轻量服务器 + 备案(PocketBase,数据自主)。

**本地知识库开源候选**(许可已核实):Obsidian+Quartz(MIT,个人知识发布)、SiYuan 思源(AGPL,本地免费)、BookStack(团队 wiki)——整合以 PocketBase 为主轴,其余按需借鉴写作流。

## 九、参考来源(免费/价格核实,2026-09-12)
- PocketBase LICENSE(MIT)与仓库活跃度:github.com/pocketbase/pocketbase(LICENSE 原文 + API)
- Supabase:supabase.com/pricing;Neon:neon.com/pricing;Cloudflare D1:developers.cloudflare.com/d1/platform/pricing
- 腾讯云 CloudBase:cloud.tencent.com/product/tcb(2026-01-16 免费体验政策调整公告见页面提示)
- LeanCloud:leancloud.cn/pricing(开发版免费+半小时休眠,docs.leancloud.cn 云引擎平台文档)
